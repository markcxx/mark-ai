# Flutter 原生复刻进度

验收基准仍是现有 Web 组件，不以“能运行”代替视觉与交互验收。更新于 2026-09-09。

## 本轮范围

- **禁用** Android PDF、Office、Word 实时预览。文件仍可生成、下载、分享；预览判断同时检查扩展名和 MIME，入口有统一保护。
- **管理端暂不做**，不计入本轮任务。
- 原生 Flutter 保留为应用界面。仅生成的 HTML、ECharts、Mermaid、Markmap 使用隔离的文档渲染表面；没有改成网页套壳。

## 已补齐的功能与证据

| 范围 | 当前实现 | 验证 |
| --- | --- | --- |
| ECharts | 提取原 Web 配置校验、主题和配色代码；原生标题栏、主题选择、展开、复制、PNG 下载 | Android 浅色/深色渲染、图片解码与导出通过 |
| Mermaid | 原 Web 校验与 Mermaid 配置；拖动、缩放、适应画布、展开、复制、SVG/PNG 下载 | Android 两种主题渲染及导出通过 |
| Markmap | 原 Web 节点限制、样式与折叠逻辑；缩放、展开/收起、SVG/PNG 下载；导出重建完整树 | Android 两种主题及导出通过 |
| 会话导出 | 图片/JSON 预览及系统保存；与 Web 相同 JSON schema；图表先转图片再合成；完整消息列截图 | 已验证导出包含公式、图表和末尾消息，无空白图表 |
| 图片与文本文件 | 生成图片移除多余的 Material 列表行；点击进入统一预览、缩放和下载；文本预览保留 | 预览策略测试通过；现有上传、下载、删除 API 保留 |
| Markdown/公式 | 正文渲染拆为可复用组件；矩阵、分段、对齐、积分、求和；表格/列表保留公式和引用；提示块与任务复选框 | 复杂 TeX 解析、320px 表格和嵌套内容测试通过 |
| 流式动画 | fade 为新增字形 180ms 淡入；代码、表格、公式不淡入；none/smooth 与减少动画设置分别生效 | 渲染更新测试及现有流式控制器测试通过 |
| 引用与来源 | 与 Web 相同的结构化编号、URL 去重和补充编号；编号点击弹层、摘要、日期、外链；来源列表展开 | 修复未知编号造成的解析循环；原生两种主题交互通过 |
| 翻译 | 默认收起；展开颜色、间距和中性卡片；仅显示完整译文；替换后保留当前展开状态 | 默认/展开/收起/替换及加载 Toast 测试通过 |
| 语音 | 使用当前音色；600 字符分句、播放器、暂停/继续、缓存重播、分段进度、取消旧请求、错误提示 | 模拟器音频插件 PCM 加载/暂停/继续/结束通过；请求、缓存和取消竞态测试通过 |

## 登录态和消息操作（上一轮已处理）

- 测试包已独立为 `com.markai.markai_mobile.qa`。Flutter drive 结束会卸载目标，之前共用包名是登录态丢失的确定原因之一；日常 App 不再被测试清除。
- 独立 QA 包已验证加密 Cookie 写入、强制终止进程、重新启动后的恢复。
- 消息操作按用户要求默认隐藏，点击消息显示，点击其他区域隐藏；保留操作区域高度。复制成功显示“消息已复制”。
- 原有三个 pnpm Android 命令不变。

## 验证记录与边界

- Flutter 单元/组件测试目前 55 项通过；新增输出契约及语音测试在 `mobile/test/output_contract_test.dart`、`speech_playback_test.dart`。
- Android 整合回归入口：`mobile/integration_test/rendering_suite_test.dart`，覆盖生成物、会话图片导出、引用/翻译/语音、公式/思考/Toast、菜单/键盘/编辑；首轮 6 个场景全部通过；生成物放大弹层及不完整图表源码恢复另行回归通过。
- 导出样本：`mobile/qa/android-conversation-export.png`、`android-echarts-*-export.png`、`android-mermaid-*-export.png`、`android-markmap-*-export.png`。
- 原生截图：`mobile/qa/android-output-states-*.png`、`android-source-popover-*.png`；Web 对照工具继续复用原 React 组件，不修改当前 Next 开发服务器。
- 测试使用虚构账号、固定响应和本地音频，没有读取或使用真实账号密码。以上不能宣称已验证所有远端模型、文件存储、翻译及语音服务的真实组合。
- 长会话 PNG 为避免 GPU 超限，将最长边限制在 8192px，保留全部消息；极长会话会降低分辨率。
- 像素级全应用验收仍按 `component-parity.md` 记录。本文“通过”指列明的测试范围，不代表所有可能的模型输出或全部界面已经逐像素一致。

## 后续未纳入本轮的项目

1. Google/GitHub 原生登录、完整新用户引导与完整公开分享页。
2. 其他界面的全状态对照：设置、插件、个人资料、侧栏与模型选择等。
3. 全服务端环境的登录 → 真实生成/工具 → 文件存储 → 重新加载回归。
4. 管理端按要求暂停；PDF/Office/Word 实时预览按要求禁用，不再列作待补功能。
